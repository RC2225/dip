# set(SOURCE_FILES videotest.cpp)

# # Add executable target with source files listed in SOURCE_FILES variable
# add_executable(videotest ${SOURCE_FILES})

set(SOURCE_FILES hello.cpp)

# Add executable target with source files listed in SOURCE_FILES variable
add_executable(hello ${SOURCE_FILES})
